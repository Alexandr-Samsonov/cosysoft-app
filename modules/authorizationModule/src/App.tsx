import React from 'react';
import { Typography } from '@material-ui/core'

const App = () => (
        <div>
            <Typography color="primary">Auth Module</Typography>
        </div>
    );

export default App